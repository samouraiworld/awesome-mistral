# Repository audit — 2026-09-07

## Scope and method

Weekly review following [MAINTENANCE.md](../MAINTENANCE.md), starting from `main` at
`f8b6f4c` (branch `docs/weekly-refresh-2026-09-07`). Reviewed the previous audit
(2026-09-04), all open pull requests and issues, remote branches, and the full
tracked README against CONTRIBUTING.md's selection criteria and format rules.

**Network limitation (material, disclosed up front):** this run's execution
environment enforces an organization egress policy that blocks `mistral.ai`,
`docs.mistral.ai`, `ollama.com`, and `reddit.com` outright (confirmed via direct
fetch attempts, each returning an explicit egress-block, not a timeout or content
issue). This is the same class of restriction PR #21 hit in June. As a result,
this review could **not** independently verify: new `mistral.ai/news` posts, the
API changelog/release-notes/security-advisories, the model catalog/pricing
snapshot, or the two Reddit link exceptions. `github.com` (including the GitHub
REST API) was fully reachable, so all GitHub-sourced verification below is live
and direct. Nothing in this audit's changes depends on a source that could not be
reached — see "Deliberately not touched" below for what that excludes.

## Live GitHub verification (72 repositories checked)

Every repository referenced in the README was queried live via the GitHub API
(stars, `archived`, `pushed_at`). Full per-repo table available on request; this
audit records only the changes.

### Star badges refreshed

| Entry | Was | Now | Live count |
| --- | --- | --- | --- |
| vLLM | ⭐ 90k+ | ⭐ 91k+ | 91,164 |
| RocketRide | ⭐ 7k+ | ⭐ 8k+ | 8,421 |
| Open WebUI | ⭐ 150k+ | ⭐ 151k+ | 151,209 |

All other 51 previously badged entries were re-checked and remain in their
current bucket (`floor(stars/1000)` unchanged) — no further edits needed.

### New badges added (crossed the 5,000-star threshold)

| Entry | Live count |
| --- | --- |
| GGUF (`ggml-org/ggml`) | 15,302 → badged ⭐ 15k+ |
| Unsloth Tutorials (`unslothai/notebooks`) | 5,660 → badged ⭐ 5k+ |

`mistralai/mistral-vibe` was also checked (it sat at 4,899 stars at the last
review): now 4,926 stars, still under the 5,000 threshold, so it remains
unbadged. Likely to cross soon; flagged as a watch item again.

### Archived-flag audit

All six repositories the README already flags as archived (`mistral-inference`,
`text-generation-inference`, `AutoGPTQ`, `bigcodebench`, `AutoAWQ`,
`mistral-finetune`) were re-confirmed `archived: true` on GitHub. No repository
in the README flipped to `archived: true` without already being flagged.
`torchtune` was independently re-confirmed **not** archived (`archived: false`),
consistent with the README's existing "no longer maintained" (not "archived")
wording; its default-branch README still opens with the "Torchtune is no longer
actively maintained" banner it has carried since April.

### Staleness review

`voxtral.c` (last push 2026-02-15) and `evalplus` (last push 2025-10-02) were
re-verified against their existing "activity watch" notes in the README — dates
still match, no change needed.

`ExLlamaV2` (`turboderp-org/exllamav2`) last pushed 2026-03-04, three days past
the six-month screening window and previously flagged in the 2026-09-04 audit as
"at the six-month review boundary." Per CONTRIBUTING.md, six months past
activity triggers a *review*, not removal (removal applies after one year with
no meaningful maintenance), so the entry is kept. Added an activity-watch note
with the exact push date so the next review can reassess without re-deriving it.

`openai/human-eval` (last push 2025-01-17, ~20 months) was checked but needs no
change: the README already describes it as a "historical code-generation
benchmark accompanying the 2021 paper; retained for reproducibility," matching
CONTRIBUTING's explicit historical-resource exception — it was never presented
as an actively maintained recommendation.

## Content, format and duplicate audit

- `python3 scripts/check_content.py`: 0 errors (after adding this audit file).
- `python3 -m unittest discover -s scripts -p 'test_*.py'`: 5 tests passed.
- `markdownlint-cli2 0.21.0`: 6 Markdown files, 0 errors.
- `git diff --check`: passed, no whitespace issues.
- Table-of-contents anchors re-derived from every `##` heading: all 20 resolve
  correctly against the Contents list; no drift.
- Duplicate-URL sweep: 11 URLs appear more than once in the document. Each was
  inspected in context; all are the contextual cross-references CONTRIBUTING.md
  explicitly permits (e.g. `vLLM`/`mistral.rs`/`Unsloth`/`axolotl`/`TRL` cited
  both as primary entries and as suggested alternatives next to archived/legacy
  entries; `docs.mistral.ai/models` and the changelog cited both in prose and as
  a primary listing). No duplicate *primary* listing of the same resource found.
- Full lychee link sweep could not be run locally (the `lychee` binary is not
  installed in this environment and its release host was not reachable to
  fetch it); relying on the repository's own CI ("Links and anchors" workflow,
  which runs with full network access) to re-verify all ~260 URLs on the PR, as
  it did successfully on every prior audit PR. Spot-checked three entries
  directly and confirmed each resolves to the described content: the GGUF
  format spec page, the Unsloth notebooks repository (confirmed it contains
  Mistral-family notebooks), and — indirectly, via the archived-repo check —
  every repository link already covered by the GitHub API sweep above.

## What's New / official sources — not touched this run

Because `mistral.ai` and `docs.mistral.ai` were unreachable from this
environment (see network limitation above), the "Latest Updates" section, the
"Snapshot checked on 2026-09-04" model table note, the SDK/Vibe release check,
and the security-advisory check were **not** re-verified this run and are left
exactly as the 2026-09-04 audit recorded them. Two of the three checks that
depend on GitHub (Vibe release tag, Python/TypeScript SDK release tags) were
independently confirmed unchanged via the GitHub API by a background check
during this run: `mistral-vibe` latest release is still `v2.25.0` (2026-09-04),
`client-python` still `v2.9.4`, `client-ts` still `v2.6.4` — no newer release
exists on any of the three as of this review. Google Cloud's own blog was
reachable and shows no new Mistral-related post. Microsoft's, NVIDIA's, and
AWS's official channels were not reachable and were not checked.

**This is a real gap, not a "nothing new" finding** — the primary source for
Mistral's own announcements could not be consulted at all. The next review must
explicitly cover the full window from 2026-09-04 through its own run date
against `mistral.ai/news/`, per MAINTENANCE.md's instruction to cover the entire
gap after a missed source check, rather than assuming a quiet week.

## Link exceptions

`maintenance.json`'s three exact-URL exceptions (X account, two Reddit
communities) were last checked 2026-09-04 and expire 2026-09-11. `reddit.com`
was unreachable from this environment this run, so the exceptions could not be
independently re-verified; `x.com` was not attempted given the pattern. They are
left unchanged (not yet expired) but must be re-verified before 2026-09-11 by a
review with the network access to do so, or removed if they can no longer be
confirmed.

## Pull requests, issues, and repository hygiene

- Open pull requests: **0**. Open issues: **0**.
- No pending `docs/weekly-refresh-*` or maintenance PR existed from a prior run,
  so this review opens a fresh PR from a fresh branch rather than stacking onto
  one.
- Two remote branches held only already-merged history and were not
  auto-deleted after merge: `chore/weekly-maintenance-2026-09-04` (PR #22,
  merged 2026-09-04) and `docs/weekly-refresh-2026-08-24` (PR #21, merged
  2026-08-31). Both were confirmed fully merged into `main` via
  `git merge-base --is-ancestor` before any deletion attempt.
  Deletion via `git push origin --delete` failed with an HTTP 403 from GitHub
  itself (not the network proxy), and no available tool in this environment can
  delete a remote branch through the GitHub API. **Left for a maintainer to
  delete manually** — flagged in the PR and the final task summary rather than
  retried, per this task's own instruction not to force through a permission
  denial.
- No duplicate issues to consolidate (none open).
- No PR/issue currently fails the quality bar (none open to evaluate).

## Curation decisions this round

No new resource was added or removed. No submission was pending review. The
only content changes are the verified star-count/badge refresh and the
ExLlamaV2 activity note above.

## Next review

Cover the full gap back to 2026-09-04 against `mistral.ai/news/`,
`docs.mistral.ai` (changelog, release notes, security advisories, model
catalog/pricing), and the Microsoft/NVIDIA/AWS official channels, from an
environment that can reach them. Re-verify the X and Reddit link exceptions
before their 2026-09-11 expiry. Re-check `mistral-vibe`'s star count (4,926,
approaching the 5k badge threshold) and `ExLlamaV2`'s upstream activity.
